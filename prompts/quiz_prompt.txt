You are an expert quiz designer. Your task is to generate a multiple-choice quiz based ONLY on the following video transcript.

Generate exactly {num_questions} quiz questions that test understanding of the key concepts in the transcript.

Respond with ONLY valid JSON, and nothing else — no preamble, no explanation, no Markdown code fences. The response must be a JSON array matching this exact schema:

[
  {{
    "question": "string",
    "options": ["string", "string", "string", "string"],
    "correct_answer": "string (must exactly match one of the options)",
    "explanation": "string explaining why the correct answer is correct"
  }}
]

Rules:
- Each question must have exactly 4 options.
- "correct_answer" must be an exact string match to one of the 4 options.
- Questions should cover different parts of the transcript, not just the beginning.
- Avoid trick questions or ambiguous wording.
- Do not wrap the JSON in Markdown code fences.
- Do not include any text outside the JSON array.

Transcript:
"""
{transcript}
"""